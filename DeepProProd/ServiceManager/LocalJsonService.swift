//
//  LocalJsonService.swift
//  DeepProProd
//
//  Created by Mushtaque Ahmed on 1/15/18.
//  Copyright © 2018 Mushtaque Ahmed. All rights reserved.
//

import UIKit

class LocalJsonService: NSObject {

    /*
    func parseLevelJson() {
        do {
            if let todoJSON = try JSONSerialization.jsonObject(with: , options: []) as? [String: Any],
                let todo = Todo(json: todoJSON) {
                // created a TODO object
                completionHandler(todo, nil)
            } else {
                // couldn't create a todo object from the JSON
                let error = BackendError.objectSerialization(reason: "Couldn't create a todo object from the JSON")
                completionHandler(nil, error)
            }
        } catch {
            // error trying to convert the data to JSON using JSONSerialization.jsonObject
            completionHandler(nil, error)
            return
        }
    })
    }
    */
    
}
